# AFK Monitor Config
# Welcome! This file controls the various options for AFK Monitor.
# There are a lot of settings and all are optional, but if you want to get started quickly:
# - Set 'WebhookURL' and 'UserID' in the [Discord] section to enable Discord output
# - Tweak the settings in [LogLevels] to control what gets logged and where to
# Feel free to read over the other settings at your leisure, and good hunting :)

[Settings]
# By default journals are loaded from your user folder
# To override this change JournalFolder below, e.g.:
# JournalFolder = '\\SOMENETWORKSHARE\Users\me\Saved Games\Frontier Developments\Elite Dangerous'
JournalFolder = ''
# UseUTC = false (default) uses local timezone, set true for UTC (in-game time)
UseUTC = false
# LiveStatus displays a persistent status line with realtime stats (Default: true)
LiveStatus = true
# DynamicTitle uses the window title to display some realtime stats (Default: true, only works on Windows OS)
DynamicTitle = true
# WarnKillRate sends a warning if kills/hour drop below set amount (Default: 20, log level 'KillRate')
WarnKillRate = 20
# WarnNoKills is time in minutes since last kill before sending a warning (Default: 20, log level 'NoKills')
WarnNoKills = 20
# PirateNames shows name of pirate on kills and cargo scans (Default: false)
PirateNames = false
# BountyFaction shows faction name on each kill (Default: false)
BountyFaction = false
# BountyValue shows credits earned on each kill (Default: false)
BountyValue = false
# ExtendedStats shows extra stats e.g. various counts and recent average rates (Default: false)
ExtendedStats = false
# MinScanLevel specifies a minimum scan level for outgoing ship scans (Default: 1)
MinScanLevel = 1

[Discord]
# Use the settings below to enable Discord webhook integration
# Webhook URL is located in Discord > Edit Channel > Integrations > Webhooks, e.g:
# WebhookURL = 'https://justanexample.fake/api/webhooks/lotsofnumbers/lotsoflettersnumbersandsymbols'
WebhookURL = ''
# User ID can be found by sending \@yourdiscordusername on Discord (use only the numbers), e.g.:
# UserID = 745279460191739562
UserID = 0
# PrependCmdrName adds the active commander name to the start of Discord messages (Default: false)
PrependCmdrName = false
# ForumChannel specifies webhook is for a forum channel and enables thread support (Default: false)
ForumChannel = false
# ThreadCmdrNames = true include the commander name in the thread name along with the start time (Default: false)
ThreadCmdrNames = false
# Timestamp = true (default) appends a timestamp to Discord messages, set false to disable
Timestamp = true
# Identity = true (default) provides default name and avatar for webhook, set false to use user-set
Identity = true

[LogLevels]
# 0 = Not logged
# 1 = Terminal
# 2 = Terminal & Discord
# 3 = Terminal & Discord with user ping
ScanIncoming = 1        # Incoming cargo scans by pirates
ScanEasy = 1            # Outgoing scans of 'easy' ships (Sidewinder etc.)
ScanHard = 2            # Outgoing scans of 'hard' ships (Python etc.)
KillEasy = 2            # Kills (bounty or bond) of easy ships
KillHard = 2            # Kills (bounty or bond) of hard ships
FighterHull = 2         # Fighter hull damage (20% increments)
FighterDown = 3         # Fighter destroyed
ShipShields = 3         # Ship shield failure
ShipHull = 3            # Ship hull damage (20% increments)
Died = 3                # Ship destruction
CargoLost = 3           # Cargo stolen
BaitValueLow = 2        # Insufficient cargo value messages
SecurityScan = 2        # Police ships being scanned
SecurityAttack = 3      # Police ships warning they are attacking you
FuelReport = 1          # Fuel use and time remaining
FuelLow = 2             # Fuel level low (under 20%)
FuelCritical = 3        # Fuel level critical (under 10%)
Missions = 2            # Kills completed for a mission
MissionsAll = 3         # Kills completed for all missions
Merits = 0              # Merits gained (powerplay-only)
NoKills = 3             # Warning of no kills for configured time ('WarnNoKills' in Settings)
KillRate = 3            # Warning of low kill rate per hour ('WarnKillRate' in Settings)
# Stat summaries are collated into one message every 10 kills and on exit
# Levels work the same, with 3 in any category adding a user ping to the message
SummaryScans = 0        # Incoming cargo scans and rate/h
SummaryKills = 2        # Kills and rate/h from bounties or bonds
SummaryFaction = 0      # As above but for the most-killed faction
SummaryBounties = 2     # Credits earned from bounties or bonds
SummaryMerits = 2       # Merits (powerplay, only shown if merits earned)

# Custom profiles (advanced)
# You can set additional profiles to override settings as per the examples below
# - Profiles are loaded automatically if they exactly match the loaded commander name
# - Alternatively they can be loaded by passing {-p <profilename>} via command line or shortcut
#
# ["ALT COMMANDER NAME"]
# Discord.WebhookURL = "https://discord.com/api/webhooks/myaltwebhook"
# LogLevels.ScanHard = 1
#
# [AnotherProfile]
# Discord.WebhookURL = "https://discord.com/api/webhooks/myaltwebhook"
# LogLevels.ScanHard = 1
#
# The first example above would be loaded automatically when running CMDR ALT COMMANDER NAME
# The second could be loaded manually with {afk_monitor.exe -p AnotherProfile}
# 
# Note: Commander name, profile, category and setting names are all case-sensitive